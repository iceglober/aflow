# v2.18.3

Released: 2026-04-13

## Changes

### Fixed
- AskUserQuestion dispatch keys now exactly match option labels (including "(Recommended)" suffix), eliminating fuzzy-match failures when the model tries to route the user's selection
- `/quick-review` NEEDS FIXES verdict now has explicit AskUserQuestion options and Skill() dispatch (was vague "same as /deep-review")
- `/build-loop` now has explicit post-`/loop` transition instructions so the model knows to proceed to "Epic Complete" after all tasks finish

### New
- `cross-refs.test.ts` regression test validates that every AskUserQuestion dispatch key has a matching option label across all skills
